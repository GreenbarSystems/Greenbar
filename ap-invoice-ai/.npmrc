# Inngest declares an optional peer on @sveltejs/kit, which npm tries to satisfy
# and pulls a vite@8 that conflicts with vitest's vite@5. We don't use SvelteKit,
# so relax strict peer resolution. Required for both `npm install` and CI `npm ci`.
legacy-peer-deps=true
